function showForm(){

	var selopt = document.getElementById('present_branch').value;
	window.alert(selopt);

	        if (selopt == AE B.Tech) {
	            document.getElementById("preference_1").style.display = "block";
	        }
}
